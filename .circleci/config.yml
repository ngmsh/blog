version: 2
jobs:
  build:
    docker:
      # specify the version
      - image: circleci/golang:1.9

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    #### TEMPLATE_NOTE: go expects specific checkout path representing url
    #### expecting it in the form of
    ####   /go/src/github.com/circleci/go-tool
    ####   /go/src/bitbucket.org/circleci/go-tool
    working_directory: /go/src/github.com/ngmsh/blog-system
    steps:
      - checkout
      - run:
          name: Hugo Install
          command: |
              go get -v github.com/spf13/hugo
              git config --global user.name "Taro"
              git config --global user.email "crunch.the.nigamushi@gmail.com"
      - run:
          name: Build
          command: |
              git clone https://github.com/ngmsh/blog.git
              cd blog
              git submodule init
              git submodule update
              git submodule foreach git pull origin master
              hugo
              # cd ../
              # git clone https://github.com/ngmsh/ngmsh.github.io.git
              # cd ngmsh.github.io
              # rm -rf *
              # cp -r ../blog/docs/ .
              # git add .
              # git commit -m "publish by ci"
              # git remote set-url origin https://github.com/ngmsh/ngmsh.github.io
              # git push origin master
